import { EcommerceDirectiveDirective } from './ecommerce-directive.directive';

describe('EcommerceDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EcommerceDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
